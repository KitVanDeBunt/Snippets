using UnityEngine;
using System.Collections;
namespace BigBossBattle {
    public class Chain : HitTrigger {
        /*void OnTriggerEnter2D(Collider2D other) {
            if (other.tag == "Player") {
                Debug.Log("ChainHitPlayer");
                other.gameObject.GetComponent<PlayerState>().Hit();
            }
        }*/
    }
}
